# 方法center通过在两边添加填充字符（默认为空格）让字符串居中。
字符串 = '努力挣钱'
print(字符串.center(39))

# 方法find在字符串中查找子串。如果找到，就返回子串的第一个字符的索引，否则返回-1
字符串 = '努力挣钱'
字符串索引 = 字符串.find('挣')
print(字符串索引)

# join是一个非常重要的字符串方法，其作用与split相反，用于合并序列的元素。
字符串 = ['努', '力', '挣', '钱']
sep = '+'
print(sep.join(字符串))

# 方法lower返回字符串的小写版本。
字符串 = 'ABC'
print(字符串.lower())

# 方法replace将指定子串都替换为另一个字符串，并返回替换后的结果。
字符串 = '努力挣钱'
print(字符串.replace('挣', '赚'))

# split是一个非常重要的字符串方法，其作用与join相反，用于将字符串拆分为序列。
字符串 = '努+力+挣+钱'
列表 = 字符串.split('+')
print(列表)

#方法strip将字符串开头和末尾的空白（但不包括中间的空白）删除，并返回删除后的结果。

字符串 = ' 努力挣钱 '
print(字符串.strip())

#方法translate与replace一样替换字符串的特定部分，但不同的是它只能进行单字符替换。
#这个方法的优势在于能够同时替换多个字符，因此效率比replace高

字符串 = '努力挣钱'
转换表=str.maketrans('努力','快乐')

print(字符串.translate(转换表))


